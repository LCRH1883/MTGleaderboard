<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/assign_player_button"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:background="@drawable/option_button_bg"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="12dp">

    <ImageView
        android:id="@+id/assign_player_avatar"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:background="@drawable/friend_avatar_background"
        android:clipToOutline="true"
        android:contentDescription="@string/profile_avatar"
        android:duplicateParentState="true"
        android:padding="1dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_user"
        android:tint="@color/option_button_text_color" />

    <TextView
        android:id="@+id/assign_player_label"
        style="@style/Header3"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/default_padding_half"
        android:layout_weight="1"
        android:duplicateParentState="true"
        android:textColor="@color/option_button_text_color"
        android:textSize="16sp" />

</LinearLayout>
